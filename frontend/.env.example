# Frontend environment overrides. Copy to `.env.local` to use.
# Defaults are baked into src/lib/features.ts.

# Show TCP/Network connection mode and mDNS discovery in the UI.
# Set to "true" once the firmware supports WiFi again. Defaults to false.
VITE_ENABLE_WIFI=false
