# Progress Log - PWDTimer Modernization

(Use this file as an append-only log. Each iteration should be a small, reviewable chunk of work.)

## Iteration 1 — Racers page polish (2026-03-11)

### What was implemented
- Improved drag-and-drop UX by allowing dragging the current multi-selection and dropping onto “All” to unassign racers from groups.
- Updated CSV export: when viewing “All racers” with no search filter, use the backend `/api/export/csv` output to ensure a consistent export.

### Files created
- N/A

### Git commits
- 8845565 fix(frontend): racers dnd + csv export

### Test results
- Frontend: `npm run lint` ✅
- Frontend: `npm run build` ✅

### Notes
- This was an outstanding local change; committed to keep the working tree clean before implementing the Race page.


## Iteration 2 — Race page realtime display (2026-03-11)

### What was implemented
- Implemented the Race page (`/race`) with a prominent current-heat header and per-lane cards (4–8 lanes).
- Wired real-time race state + lane times + places via WebSocket (`/ws`) and added visual state badges (Ready/Set/Racing/Finished).
- Added timer controls (Arm/Reset/Set lanes), a connection status indicator + reconnect button, and fullscreen mode for projector display.
- Added optional start/finish sound effects (Web Audio API) and fixed the websocket hook to avoid reconnecting after an explicit disconnect/unmount.

### Files created
- N/A

### Git commits
- 98285bb feat(frontend): race page realtime display

### Test results
- Backend: `python3 -m unittest discover -s tests -p 'test*.py' -v` ✅
- Frontend: `npm run lint` ✅
- Frontend: `npm run build` ✅

### Notes
- The frontend dev server proxies `/api` and `/ws` to the backend (see `frontend/vite.config.ts`).


## Iteration 3 — Results page standings (2026-03-11)

### What was implemented
- Implemented the Results page (`/results`) with race selection, group filter, sortable standings, and top-3 highlighting.
- Computed per-lane best times, average, and best time from completed heats; added expandable per-racer run history (heat/lane/place/time).
- Added PDF export download (backend `/api/races/{id}/export/pdf`) and print-friendly output (hide nav/sidebar when printing).
- Added summary stats (fastest single time, closest finish, participation + heat completion counts).

### Files created
- N/A

### Git commits
- cb47899 feat(frontend): results page standings

### Test results
- Backend: `python3 -m unittest discover -s tests -p 'test*.py' -v` ✅
- Frontend: `npm run lint` ✅
- Frontend: `npm run build` ✅

### Notes
- When repeat heats create multiple runs in the same lane, the standings use the best time for that lane.


## Iteration 4 — Certificates page (2026-03-11)

### What was implemented
- Completed the Certificates page (`/certificates`) with certificate type selection (winner / participation / custom message), recipient selection (all / group / individual), and event customization (name/date/issued-by/message).
- Added PDF preview flows (sample + current selection) and actions to download or open the generated PDF for printing.
- Added basic generation progress UX (busy state + estimated page count where applicable).
- Updated the PRD to mark the certificates-page milestone as complete.

### Files created
- N/A

### Git commits
- N/A

### Test results
- Frontend: `npm run lint` ✅
- Frontend: `npm run build` ✅

### Notes
- The “Custom message” option uses the participation-certificate layout with an additional message field.


## Iteration 5 — Settings page (2026-03-11)

### What was implemented
- Implemented the Settings page (`/settings`) with Serial vs Network connection configuration.
- Added connect/disconnect controls with live status, serial port refresh, and mDNS discovery helpers.
- Added lane count configuration (4/6/8) with “Apply to timer” (`SET_LANES`) plus local persistence.
- Added race timing parameters (min/max valid time) stored in localStorage.
- Added theme controls (light/dark) and sound controls (enable, volume, optional custom URLs) stored in localStorage.
- Wired the Race page to use the stored sound settings (enable/volume/custom URLs).

### Files created
- frontend/src/lib/settings.ts

### Git commits
- N/A

### Test results
- Frontend: `npm run lint` ✅
- Frontend: `npm run build` ✅

### Notes
- “Sync with backend” is implemented for connection + lane count; other settings are currently client-side only.
