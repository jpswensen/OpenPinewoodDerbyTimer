# Progress Log - PWDTimer Modernization


## Iteration 1 — Python legacy review (2026-03-11)

### What was implemented
- Reviewed the legacy Python/PyQt5 app (UI structure, data model, heat scheduling, and timing UI flow)
- Documented file formats (JSON save/load and CSV import)
- Documented the serial/TCP protocol implementation and called out key gaps relevant to modernization

### Files created
- docs/01_python_implementation_review.md
- plans/prd-pwdtimer.json
- progress.txt

### Git commits
- f0f77a1 chore: add ralph tracking files
- 20f8007 research(docs): review legacy python app

### Test results
- N/A (research/documentation only)

### Notes
- Legacy TCP connect/parsing appears incomplete/buggy; the modern stack should define and strictly test the framing + command set


## Iteration 2 — Firmware/hardware legacy review (2026-03-11)

### What was implemented
- Reviewed the ESP8266 firmware variants (serial 4-lane + TCP sketches), documenting pin mappings and timing/state-machine behavior
- Reviewed the ESP32 PlatformIO firmware (FreeRTOS tasks, queue-based comms, SoftAP + mDNS) and documented the supported command set
- Reviewed hardware design artifacts (Eagle schematic/board files) and the Digikey parts list
- Documented protocol compatibility risks (missing numLanes field in legacy messages, strict RESET parsing, incomplete TCP variants)

### Files created
- docs/02_firmware_hardware_review.md

### Git commits
- e8d17d6 research(docs): review legacy firmware/hardware

### Test results
- N/A (research/documentation only)

### Notes
- The ESP8266 TCP sketches appear incomplete/buggy (interrupt hookup commented out; 8-lane variant has ISR index/out-of-bounds risk). The ESP32 firmware is the best baseline for modernization.


## Iteration 3 — Infrastructure scaffold (2026-03-11)

### What was implemented
- Created FastAPI backend scaffold with CORS and `/api/health`
- Scaffolded Vite + React + TypeScript frontend with routing, TanStack Query provider, Radix Themes, and Tailwind CSS (dark mode via `class`)
- Added `start.sh` to run backend+frontend together (auto-installs backend deps if missing)

### Files created
- backend/ (FastAPI scaffold + requirements.txt)
- frontend/ (Vite scaffold + Tailwind/PostCSS config)
- start.sh

### Git commits
- 25417d2 chore(infra): scaffold backend and frontend

### Test results
- Frontend: `npm run build` ✅
- Backend: `GET /api/health` ✅

### Notes
- `start.sh` expects `python3` to be a suitable environment (recommended: activate the repo venv before running).


## Iteration 4 — Backend DB models (2026-03-11)

### What was implemented
- Added async SQLAlchemy + SQLite database layer (engine/session helpers and init/create-all)
- Implemented core ORM models (Group, Racer, Race, Heat, HeatLane, RaceResult) and Pydantic schemas
- Wired backend startup to initialize the schema automatically
- Added model-level unit tests using `unittest` + temporary SQLite database

### Files created
- backend/app/models/__init__.py
- backend/app/models/database.py
- backend/app/models/models.py
- backend/app/models/schemas.py
- backend/app/models/init_db.py
- backend/tests/test_models.py

### Git commits
- 747bdca feat(db): add async SQLAlchemy models

### Test results
- Backend: `python -m unittest discover -s tests -p 'test*.py' -v` ✅

### Notes
- Times are stored as integers in microseconds where applicable to preserve precision.


## Iteration 5 — APIs + real-time WebSocket (2026-03-11)

### What was implemented
- Added Groups + Racers CRUD APIs, bulk racer creation, and CSV import/export
- Added race + heat APIs including deterministic round-robin scheduling, repeats, and result recalculation
- Added serial/TCP + mDNS connection layer with a framed `$...*` status parser and connection status endpoints
- Added `/ws` WebSocket endpoint with event types: `connection_status`, `race_state`, `lane_times`, `heat_complete`
- Implemented an internal EventBus for broadcasting to all connected WebSocket clients

### Files created
- backend/app/routers/{groups,racers,import_export,races,connection,websocket}.py
- backend/app/services/{heat_scheduler,race_results,timer_protocol,tcp_connection,serial_connection,mdns_discovery,connection_manager,event_bus}.py
- backend/tests/{test_api_participants,test_api_races,test_communications,test_websocket}.py

### Git commits
- 1aeff30 feat(backend): add APIs and real-time websocket

### Test results
- Backend: `python3 -m unittest discover -s tests -p 'test*.py' -v` ✅

### Notes
- WebSocket auth is optional: set `PWD_TIMER_WS_TOKEN` and connect with `/ws?token=...`.


## Iteration 6 — PDF results export (2026-03-11)

### What was implemented
- Added ReportLab-based PDF generator for race results with a professional header, metadata, and tabular layout
- Added `GET /api/races/{id}/export/pdf` supporting group-scoped exports plus page size (letter/A4) and orientation (portrait/landscape)
- Added backend tests validating the PDF endpoint returns a valid PDF payload

### Files created
- backend/app/services/pdf_generator.py

### Git commits
- ceb101b feat(backend): export race results PDF

### Test results
- Backend: `python3 -m unittest discover -s tests -p 'test*.py' -v` ✅

### Notes
- PDF generation uses ReportLab; `weasyprint` is listed as an optional dependency (skipped on macOS via env marker).


## Iteration 7 — Certificate generation (2026-03-11)

### What was implemented
- Added ReportLab certificate generator (decorative border + seal) for winner and participation certificates
- Added `/api/certificates/preview` and `/api/certificates/generate` (batch, one certificate per page)
- Added backend tests covering preview + generation flows

### Files created
- backend/app/services/certificate_generator.py
- backend/app/routers/certificates.py
- backend/tests/test_certificates.py

### Git commits
- f37a8dc feat(backend): certificate generation

### Test results
- Backend: `python3 -m unittest discover -s tests -p 'test*.py' -v` ✅

### Notes
- Uses built-in fonts by default; will auto-register optional bundled fonts from `backend/app/assets/fonts/` if present.


## Iteration 8 — Frontend scaffold (2026-03-11)

### What was implemented
- Added app shell with sidebar layout and routes for: /, /racers, /heats, /race, /results, /certificates, /settings
- Implemented theme context with persisted light/dark mode (Tailwind `dark` class + localStorage) and Radix Theme integration
- Added typed API client + endpoint modules and an auto-reconnecting WebSocket hook
- Added basic reusable UI components: Button, Card, Table, Modal, Toast

### Files created
- frontend/src/api/
- frontend/src/components/
- frontend/src/context/
- frontend/src/hooks/
- frontend/src/pages/
- frontend/src/providers/
- frontend/src/routes/

### Git commits
- 14ccb5e feat(frontend): scaffold app shell

### Test results
- Frontend: `npm run lint` ✅
- Frontend: `npm run build` ✅

### Notes
- Vite dev server proxies `/api` and `/ws` to `http://127.0.0.1:8000`.


## Iteration 9 — Participants page (2026-03-11)

### What was implemented
- Implemented the Racers page with a Groups sidebar (create/edit/delete) and a Racers table filtered by selected group
- Added inline racer editing, add-racer validation, single delete confirmation, and bulk select/delete
- Implemented drag-and-drop to move racers between groups
- Added CSV import modal with preview + column mapping (normalizes headers and uploads to backend CSV import)
- Added CSV export for the currently displayed racers (respects group + search filters)

### Files created
- frontend/src/api/endpoints/importExport.ts

### Git commits
- 2a998d6 feat(frontend): implement racers page
- 5ce1500 chore: update prd progress

### Test results
- Frontend: `npm run lint` ✅
- Frontend: `npm run build` ✅

### Notes
- CSV parsing uses `papaparse` for robust header handling; imports are normalized to the backend’s expected header schema.


